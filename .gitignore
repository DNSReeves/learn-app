# user data — never ship (usernames, pw hashes, learning state)
learn.db
*.db
*.db.bak*
*.bak*
# runtime noise
boot.log
__pycache__/
.pytest_cache/
.venv/
.DS_Store

# P3.12: generated TTS assets — rebuild with .venv/bin/python tts_render.py
static/audio/
